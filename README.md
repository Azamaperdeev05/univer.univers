# Univer.Univers 🎓

Бұл жоба студенттерге арналған бірыңғай портал. Ол университеттердің (KSTU, KazNU және т.б.) оқу жүйелерімен интеграцияланып, сабақ кестесін, бағаларды, транскриптті және сабаққа қатысуды ыңғайлы интерфейсте көруге мүмкіндік береді.

## 🚀 Ерекшеліктері

*   **Сабақ кестесі**: Апталық сабақ кестесін көру.
*   **Бағалар**: Ағымдағы бағалар мен межелік бақылау нәтижелері.
*   **Транскрипт**: Толық оқу үлгерімі (GPA).
*   **Сабаққа қатысу**: Қалдырылған сабақтар статистикасы.
*   **Ыңғайлы интерфейс**: Заманауи және жылдам жұмыс істейтін веб-интерфейс (SPA).

## 🛠 Технологиялар

*   **Backend**: Python, aiohttp (асинхронды сервер).
*   **Frontend**: Svelte, TypeScript (build жасалып `static` папкасында орналасқан).
*   **Parsing**: BeautifulSoup4 (HTML талдау).

## 📦 Орнату және Іске қосу

### Талаптар
*   Python 3.9+
*   Git

### Жергілікті компьютерде қосу

1.  **Репозиторийді жүктеу:**
    ```bash
    git clone https://github.com/your-username/univer.univers.git
    cd univer.univers
    ```

2.  **Виртуалды ортаны құру (ұсынылады):**
    ```bash
    python -m venv venv
    # Windows:
    venv\Scripts\activate
    # Linux/Mac:
    source venv/bin/activate
    ```

3.  **Тәуелділіктерді орнату:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Серверді қосу:**
    ```bash
    python server.py
    ```
    Сервер `http://localhost:7435` мекенжайында қосылады.

## 🐳 Docker арқылы қосу

Егер Docker орнатылған болса, жобаны контейнерде оңай қоса аласыз:

1.  **Image жинау:**
    ```bash
    docker build -t univer-app .
    ```

2.  **Контейнерді қосу:**
    ```bash
    docker run -p 7435:7435 univer-app
    ```

## ☁️ Деплой (Deploy)

Жоба **Heroku**, **Render**, **Railway** немесе кез келген VPS-ке орнатуға дайын.

*   **Start Command**: `python server.py`
*   **Port**: `7435` (немесе қоршаған ортаның `PORT` айнымалысы арқылы).

## 🤝 Үлес қосу

Қате тапсаңыз немесе жаңа функция қосқыңыз келсе, Pull Request жіберуге болады.

## 📄 Лицензия

MIT License
